<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.inventory.mapper.InventoryTxLogMapper">

    <select id="findStatusByOrderNo" resultType="string">
        SELECT status
        FROM inventory_tx_log
        WHERE order_no = #{orderNo}
    </select>

    <resultMap id="InventoryTxLogResultMap" type="com.example.inventory.model.InventoryTxLog">
        <id property="orderNo" column="order_no"/>
        <result property="productId" column="product_id"/>
        <result property="status" column="status"/>
        <result property="reason" column="reason"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="findByOrderNo" resultMap="InventoryTxLogResultMap">
        SELECT order_no, product_id, status, reason, created_at, updated_at
        FROM inventory_tx_log
        WHERE order_no = #{orderNo}
    </select>

    <insert id="insert">
        INSERT INTO inventory_tx_log (
            order_no, product_id, status, reason
        ) VALUES (
            #{orderNo}, #{productId}, #{status}, #{reason}
        )
    </insert>

    <update id="updateStatus">
        UPDATE inventory_tx_log
        SET status = #{status},
            reason = #{reason},
            updated_at = CURRENT_TIMESTAMP
        WHERE order_no = #{orderNo}
    </update>

</mapper>
